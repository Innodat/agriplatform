# Configure your own executable provider runner; JSON request on stdin, response on stdout.
# No credentials are read or stored by the domain pipeline.
# Provider runner credentials belong in its own environment, never in --settings.
